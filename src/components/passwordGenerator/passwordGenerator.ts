import { Criteria } from "../../types";

/**
 * Generate password by provided criteria
 */
const generate = (length: number, criteria: Criteria): string => {
  return 'password';
}

export default generate;