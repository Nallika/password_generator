import { CriteriaEnum } from '../types';

export const MIN_LENGTH = 5;

export const MAX_LENGTH = 20;

export const DEFAULT_LENGTH = 10;
/**
 * Default selected criteria
 */
export const DEFAULT_CRITERIA = CriteriaEnum.lowercase;